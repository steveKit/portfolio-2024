//initial header animation delay
const headerDelay = 1;

//timing for new text block in home component
const intervalDelay = 10;

//timing offset for progress bars in TextBlockIndexIndicator. There is an animation delay of half this number.
const animationTimingOffset = 1;

export {
    headerDelay,
    intervalDelay,
    animationTimingOffset
};